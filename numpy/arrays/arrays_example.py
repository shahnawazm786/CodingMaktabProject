import numpy as np

#array created from list
#Create a 1-D array containing two arrays with the values 1,2,3,4,5
arr=np.array([1,2,3,4,5])
print(type(arr))
print(arr)
# array created by tuple
#Create a 1-D array containing two arrays with the values 1,2,3,4,5
arr1=np.array((1,2,3,4,5))
print(arr1)
print(type(arr1))

#Create a 2-D array containing two arrays with the values 1,2,3 and 4,5,6:
